#define CATCH_CONFIG_MAIN
#include "catch.hpp"

#include "saber.hpp"

using namespace SAT;

SCENARIO("1") {
    GIVEN("given") {
        WHEN("when") {
            THEN("then") { REQUIRE(true); }
        }
    }
}
